# Deployment Status

- Installed the Vercel CLI globally using `npm i -g vercel`.
- `VERCEL_TOKEN` environment variable is present.
- `VERCEL_TEAM` environment variable is missing, so production deployments were not attempted.

